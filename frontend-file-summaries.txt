{
  "frontend/src/App.tsx": "Root app component with ThemeProvider, auth state, and view-based routing (`dashboard`, `tool`, `analytics`, `myFiles`) — passes navigation handlers and `activeView` to Header, renders the correct page per view.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — fetching/saving the caller's user profile, fetching the user's stored files (useGetMyFiles), uploading a new file (useSaveFile), and deleting a file (useDeleteFile), with proper query invalidation on mutations.",
  "frontend/src/components/Header.tsx": "Sticky header with explicit `flex-row` on all flex containers, `flex-shrink-0` and `whitespace-nowrap` on all items to prevent wrapping, a white background wrapper on the logo image to hide PNG transparency, and proper horizontal alignment of logo, nav links, and right-side controls.",
  "frontend/src/components/Footer.tsx": "App footer with copyright and caffeine.ai attribution displayed in a single centered horizontal row, using explicit Tailwind color classes instead of vault tokens, and a purple gradient accent bar at the bottom.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog shown on first login to collect the user's display name and save it to the backend profile.",
  "frontend/src/pages/LoginPage.tsx": "Unauthenticated landing page matching Dashboard layout with hero section, 2-column tool card grid (each card triggers login), login CTA button, and features strip; uses explicit Tailwind color classes for consistent light/dark styling.",
  "frontend/src/pages/Dashboard.tsx": "Authenticated dashboard with hero section, 2-column responsive tool card grid, and features strip; uses explicit Tailwind color classes for consistent light/dark styling with individual card borders and hover effects.",
  "frontend/src/pages/ToolPage.tsx": "Wrapper page that renders the selected tool component with a back-navigation header and tool title.",
  "frontend/src/components/shared/FileUploadZone.tsx": "Reusable drag-and-drop file upload component with file picker fallback, file list display with sizes, and reorder controls for multiple files.",
  "frontend/src/components/tools/MergePDFTool.tsx": "Merge PDF tool that uses `getPDFLib()` to access the CDN-loaded pdf-lib global inside the handler, combining multiple PDFs into one downloadable file.",
  "frontend/src/components/tools/SplitPDFTool.tsx": "Split PDF tool with four modes — All Pages, Page Range, Specific Pages (comma-separated page numbers with validation), and By Page Count (sequential chunks of N pages with preview of chunk count); uses pdf-lib via getPDFLib() CDN accessor and triggers individual file downloads per chunk.",
  "frontend/src/components/tools/CompressPDFTool.tsx": "Compress PDF tool that uses pdf.js canvas-based page rasterization with standard compression settings (0.85× scale / 70% JPEG quality); the High compression option and all related state, types, and UI controls have been removed, leaving only the single Standard mode.",
  "frontend/src/components/tools/ImageToPDFTool.tsx": "Image to PDF tool using `getJsPDF()` to access jsPDF from the CDN global; embeds multiple JPG/PNG images into a single PDF.",
  "frontend/src/components/tools/PDFToWordTool.tsx": "PDF to Word tool using pure browser-native text extraction (no external library); generates an RTF file compatible with Word/LibreOffice.",
  "frontend/src/components/tools/WordToPDFTool.tsx": "Word to PDF tool using `getJsPDF()` for PDF generation and a browser-native ZIP parser to extract text from .docx files without any npm dependencies.",
  "frontend/src/components/tools/ExcelToPDFTool.tsx": "Excel to PDF tool using `getXLSX()` and `getJsPDF()` to access CDN globals; renders spreadsheet data as a styled table in a landscape PDF.",
  "frontend/src/components/tools/RotatePDFTool.tsx": "Rotate PDF tool using `getPDFLib()` to access pdf-lib's PDFDocument and degrees function from the CDN global; applies rotation to all pages.",
  "frontend/src/components/tools/PasswordProtectPDFTool.tsx": "Password protect PDF tool using `getPDFLib()` to re-save the PDF with metadata via the CDN-loaded pdf-lib global; includes password/confirm fields with visibility toggle.",
  "frontend/src/index.css": "Global styles with OKLCH color system; all CSS custom properties are now defined as complete `oklch(...)` values (not bare channel tuples) for both light and dark modes, fixing the broken `oklch(var(--token))` pattern in Tailwind. The `* { border-color }` rule uses a plain CSS variable reference instead of `@apply border-border` to avoid cascade issues.",
  "frontend/tailwind.config.js": "Tailwind configuration with Inter font and vault design tokens; all color references now use plain `var(--token)` strings instead of `oklch(var(--token) / <alpha-value>)` wrappers, which is compatible with the complete oklch values now stored in the CSS variables.",
  "frontend/index.html": "HTML entry point that loads pdf-lib, jsPDF, xlsx, and pdf.js (with worker config) from CDN as UMD globals before the React app starts.",
  "frontend/src/types/pdf-lib.d.ts": "Ambient TypeScript declarations for the `PDFLib` CDN global (including `embedJpg`/`embedPng` on `PDFDocument`) and the `pdfjsLib` CDN global (with full type coverage for `getDocument`, `PDFDocument`, `Page`, `Viewport`, and `RenderTask`), both exposed on the `Window` interface.",
  "frontend/src/types/jspdf.d.ts": "Ambient TypeScript namespace declaration for the `jspdf` CDN global, providing type coverage for the jsPDF class constructor and all methods used in the tools.",
  "frontend/src/types/xlsx.d.ts": "Ambient TypeScript namespace declaration for the `XLSX` CDN global, providing type coverage for WorkBook, WorkSheet, read(), and utils.sheet_to_json().",
  "frontend/src/types/jszip.d.ts": "Minimal ambient namespace declaration kept for reference; JSZip is no longer used since DOCX parsing uses a browser-native ZIP parser.",
  "frontend/src/lib/pdfUtils.ts": "Utility module providing `getPDFLib()`, `getJsPDF()`, and `getXLSX()` accessors for CDN-loaded globals, plus `downloadBytes`, `downloadBlob`, and `formatBytes` helpers.",
  "frontend/public/google61423bcf1843cd2d.html": "Static Google Search Console HTML verification file served at /google61423bcf1843cd2d.html with the standard verification content.",
  "frontend/src/pages/AnalyticsPage.tsx": "Analytics page showing overview stat cards (Total Operations, Files Processed, Tools Used) and a per-tool usage breakdown, with an empty-state message and a CTA to navigate back to the dashboard.",
  "frontend/src/pages/MyFilesPage.tsx": "Fully functional My Files page that lists the authenticated user's stored PDFs with name, size, and upload date; supports uploading new PDFs via ExternalBlob with a progress bar, downloading files via direct URL, and deleting files with per-item loading states, plus empty/loading/error states and toast notifications.",
  "frontend/public/sitemap.xml": "Static XML sitemap file placed in the public directory so it is served directly as a static asset (bypassing React Router), containing all major app routes for `https://pdfvaulty-dqb.caffeine.xyz` with `<loc>`, `<lastmod>`, `<changefreq>`, and `<priority>` entries.",
  "frontend/public/.ic-assets.json5": "Internet Computer asset canister configuration file that sets `Content-Type: application/xml` for `sitemap.xml` and `Content-Type: text/html` for the Google Search Console verification file, ensuring correct MIME type delivery by the IC asset canister."
}